apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: deploment-es7data1
  name: deploment-es7data1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
